# artifacts
__pycache__
.pytest*
*.egg-info
*/build/*
*/dist/*
# The panel bundle is a build artifact but is committed so the integration
# works without a Node toolchain when installed (e.g. via HACS).
!custom_components/simple_chores/frontend/dist/*
frontend/node_modules


# misc
.coverage
.vscode
coverage.xml
.ruff_cache


# Home Assistant configuration
config/*
!config/configuration.yaml